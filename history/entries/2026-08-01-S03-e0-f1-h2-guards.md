# S03 — Story [E0.F1.H2]: executable architecture guards

- **Date**: 2026-08-01
- **Branch**: `claude/scaffold-hexagonal-structure-dfq16n`
- **Scope**: `[E0.F1.H2]` (issue #3, milestone E0 — Foundations)
- **sdd-lite changes**: [e0-f1-h2-guards](../../sdd-lite/openspec/changes/e0-f1-h2-guards/) (completed — final QA: clean pass)

## Objective

Execute `[E0.F1.H2]` end-to-end as sdd-lite change `e0-f1-h2-guards` per the user kickoff: the 5 PRD
§4.5 guards executable via dependency-cruiser (setup §5.3), `check` completed per setup §5.1, all 5
rules proven red with temporary reverted violations, PR, history entry.

## Decisions

| ID | Decision | Alternatives considered | Why | Authorship |
|----|----------|-------------------------|-----|------------|
| S03-D1 | Chained (auto) stage execution; kickoff recorded as whole-change `stage_approval` (ckp-001) | Interactive per-stage pauses | Same autonomous-session pattern as S02-D1; kickoff pre-directed flow, scope, and closing requirements | `claude` |
| S03-D2 | Work on the harness-designated branch `...dfq16n` | Kickoff-named `...ao90q8` | `ao90q8` was H1's branch, already merged in PR #47; `dfq16n` was checked out from the same main tip | `claude` |
| S03-D3 | `core-no-io-libs` bans ALL Node builtins (bare + `node:` forms) besides npm-except-zod | §5.3 sketch (npm-only ban); I/O-subset ban | PRD §4.5 rule 2 is mandatory and names builtins; setup is a recommendation. Strongest extraction guarantee at zero cost today (change dec-002) | `claude` |
| S03-D4 | dependency-cruiser pinned 18.1.0 exact; rule semantics written from its published source, not the §5.3 sketch verbatim | Copying §5.3 blind | Sketch backrefs (`\1`) and bare-name whitelist are not valid dc semantics — a blind copy yields silently permissive guards (change dec-007/dec-009, risk-001) | `claude` |
| S03-D5 | typescript re-pinned 7.0.2 → 5.9.3 exact (change dec-011, protocol C consultation) | Keep 7.0.2 + add @swc/core transpiler; defer the story | dc 18.1.0 supports TS `>=2.0.0 <7.0.0`; under 7.0.2 depcruise cruised 0 modules and exited 0 — guard silently inert, check falsely green. User chose the H1 pre-authorized fallback line; TS 7.x return deferred until dc ships support | `claude→user` |
| S03-D6 | Review triage `standard`, single `reliability` lens (H1 used `risk`) | risk lens; full-4r | 109 hand-written lines < 400; dominant defect class here is a silently permissive rule = correctness; red proofs already covered the primary contracts | `claude` |
| S03-D7 | 3 WARNING review findings recorded as `info`, no fix round | Fixing regex anchoring in-story | Severity floor (review contract): WARNING never blocks or enters the fix loop; gaps only activate on layouts the PRD does not sanction. Seeded as optional follow-up story | `claude` |

Stage-level decisions dec-002..dec-011 and deviations dev-001/dev-002 live in the change artifacts
([spec](../../sdd-lite/openspec/changes/e0-f1-h2-guards/spec.md),
[execution-log](../../sdd-lite/openspec/changes/e0-f1-h2-guards/execution-log.md)) — linked, not copied.

## Deviations

- **Protocol C STOP mid-execution (risk-005 fired)** — S1's green-baseline gate revealed the dc/TS 7
  incompatibility described in S03-D5; execution paused at commit `3f2b955`, user consulted in-session,
  resumed after the dec-011 resolution. Reality refuted S02-D3's "TS 7.0.2 works for the toolchain".
- **§5.3 config sketch not verbatim-usable** (anticipated as risk-001, confirmed at design): rules were
  rewritten with source-verified dc semantics; behavior, not syntax, was the contract (spec AC-05..AC-09).
- **AC-06 full-chain red fixture adjusted (dev-002)**: appending an import after `export {};` trips biome
  `noUselessEmptyExport` before depcruise runs; the temporary edit replaced the line instead. Direct
  depcruise attribution proofs unaffected.

## Work done

- Commits (story branch, all pushed): `800626c` change open (proposal) · `bff3111` spec · `d174831`
  design · `a06f221` plan · `3f2b955` S1 blocked snapshot (protocol C) · `9e124e2` guards implementation
  (feat) · `ba3325e` 4R review ledger · `33f00f4` final QA close.
- **PR #48 opened**: `[E0.F1.H2] Executable architecture guards` (Closes #3). Not merged (user reviews).
- Verification: all 5 rules proven red with temporary violations (each naming exactly its rule, all
  reverted; negative control green), full-chain red at the depcruise step, 12 independent review probes,
  final `npm run check` exit 0 with depcruise cruising 13 modules (non-inert) and `npm run dev` exit 0.
  `npm test` still not runnable by design (vitest lands E0.F2.x).
- 4R review standard/reliability: 0 severe, 3 `info` (regex boundary-shape hardening candidates:
  [ledger](../../sdd-lite/openspec/changes/e0-f1-h2-guards/review-ledger.md)); final QA clean pass,
  AC-01..AC-13 verified ([qa-report](../../sdd-lite/openspec/changes/e0-f1-h2-guards/qa-report.md)).

## Pending and next steps

- **User**: review and merge PR #48.
- **Claude (next stories, E0)**: `[E0.F1.H3]` CI pipeline (guards enter CI), then E0.F2.x base contracts.
  Optional backlog seed from R3-001..R3-003: guard-regex hardening (`(/|$)` anchoring, depth-1 coverage).
- **Future story (post-dc TS 7 support)**: return typescript to the 7.x line (noted in the config header).

## Open questions for the user

—
