# S24 — [E5.F1.H1] ProcessRunner: port, execa adapter, and full-4r review, end to end

- **Date**: 2026-08-23
- **Branch**: `claude/e5-f1-h1-process-runner`
- **Scope**: project-state validation (E5 status, GitHub sync check), then `[E5.F1.H1]` (issue #31), third story of the E5 milestone — full sdd-lite cycle: proposal → spec (revision 2) → design → plan → ST-1..ST-4 → full-4r code review → ST-5 fix stage → final QA
- **sdd-lite changes**: [`e5-f1-h1-process-runner`](../../sdd-lite/openspec/changes/e5-f1-h1-process-runner/) (complete: `lifecycle_status: completed`, `qa_summary.verdict: pass_with_warnings`, `review_summary.verdict: pass_with_warnings` with `open_severe_findings: 0`)

## Objective

Confirm the prior session's PR #70 merge and E5 status were correctly reflected in GitHub and `history/`, then implement `[E5.F1.H1]` — the `ProcessRunner` driven port (PRD §4.3, the last unimplemented MVP port) and its `execa`-backed adapter, the second driven port of `src/core/run` after `ReviewEngine`.

## Decisions

| ID | Decision | Alternatives considered | Why | Authorship |
|----|----------|--------------------------|-----|------------|
| S24-D1 | Story chosen: `[E5.F1.H1]` `ProcessRunner` over `[E5.F1.H2]` declared validations | Both were independently unblocked; `[E5.F1.H2]` depends on `[E5.F1.H1]` | User picked `#31` directly when asked which E5 story to continue with | `user` |
| S24-D2 | Spec ran a directed, empirical re-derivation (revision 2) before design, verifying every `execa` claim by running live probes against the installed `execa@9.6.1` rather than trusting its README or the two existing engine `process-runner.ts` seams | Ratify revision 1 as-is (it was written from the seams + README) | Revision 1 turned out to have 4 wrong assumptions: `reject:false` doesn't make spawn failures reject at all (both seams' own doc comments assert the opposite — a latent defect in already-merged E4 code, recorded as `risk-005`); `maxBuffer` counts UTF-16 characters, not bytes; `isMaxBuffer` is a single global flag, not per-stream; `stripFinalNewline` defaults `true` and silently eats every capture's trailing newline. Also surfaced a real gap no revision-1 AC covered: a child that both overflows its output budget and hangs reports `timedOut: false` from execa itself | `claude` (empirical verification, ratified alongside D1-D3 at `cp-spec-approval`) |
| S24-D3 | D1 (spec): the "only declared validation scripts" guarantee lives entirely in `[E5.F1.H2]`, not in this port — `ProcessRunner` takes generic `(command, args)` and knows nothing about repo config or provenance | A narrower port taking an already-validated `DeclaredValidation` descriptor | `repos/ports/config-schemas.ts` already declares `validations: z.array(z.string())` — the list's provenance is owned by `repos`, and a port cannot verify provenance it was never given. Blast radius reduced structurally instead (command/args always separate, no shell, `cwd` must be absolute) | `claude→user` (ratified at `cp-spec-approval`) |
| S24-D4 | D2 (spec): `env` inherits the parent environment with an optional overlay, never a replacement | Require `env` explicitly (the `opencode` seam's shape) | A declared validation (`npm test`, `pytest`) needs a real `PATH`/`HOME`/toolchain env; both existing engine seams already prove overlay-on-inherit is the house behavior | `claude→user` (ratified at `cp-spec-approval`) |
| S24-D5 | D3 (spec, new in revision 2): the adapter classifies `execa`'s resolved result itself — `reject:false` means *nothing* rejects, so the resolve-vs-throw judgment cannot be delegated to the library | Use `reject:true` + try/catch | Empirically, under `reject:false` execa returns an `ExecaError` *as the resolved value* for every failure (non-zero exit, timeout, `maxBuffer` overflow, spawn failure alike) — a non-zero exit (this port's single most expected outcome) also carries `failed:true`, so a `failed`-based check is unusable | `claude→user` (ratified at `cp-spec-approval`) |
| S24-D6 | Design concentrates all six of revision-2's empirical findings into ONE pure exported function, `classifyExecaResult`, leaving the impure adapter (`process-runner-exec.ts`) trivial | Spread the logic across the adapter inline | Makes the awkward cases (overflow-then-hang, `EACCES`, signal-without-exit-code) unit-testable with zero spawned children; real-child tests (ST-4) are then reserved for what only a live process can prove (above all AC-1's reaping) | `claude` |
| S24-D7 | D-2 (design): request pre-flight is plain guard clauses in core (matching `runReview`'s existing style, `run` has never imported zod); `cwd` absoluteness is checked in the adapter via `node:path`'s `isAbsolute`, not in core | Zod schema in core, matching `history`'s pattern | `core-no-io-libs` forbids `node:path` in core; `run-review.ts:59-62` already documents the identical split for `repoPath` ("Absoluteness is validated by `createReviewWorktree`, deliberately not re-validated here") — a real in-repo precedent, not an invention | `claude` |
| S24-D8 | 4R review triaged `full-4r` | `standard` tier | 680 changed lines (over the 400-line threshold) and the diff touches process-spawning surface (shell-injection guard, spawn-failure classification) — matches the two prior E5 stories' precedent | `claude` |
| S24-D9 | R4-001 (the sole confirmed CRITICAL — `ProcessSpawnError.cause` stripped `command`/`args`/`cwd` from a genuine spawn failure) fixed in an added ST-5 before final QA | Defer the fix, proceed to final QA with the finding open | Real, deterministic observability gap directly contradicting `run-errors.ts`'s own stated "raw error preserved for observability" contract; independently re-verified by the orchestrator via direct code reading (no refuter pass needed — `evidence_class: deterministic`) before the user authorized the fix | `user` (via `review_gate`, "si, corré el plan para arreglar R4-001") |

## Deviations

- **`plan.md`'s Stage Plan `Status` column was never synced after each stage executed** (ST-1 through ST-4 all sat at `pending` in the table despite `execution-log.md` recording them completed) — caught and corrected when appending ST-5, all five rows now reflect actual status. Process gap for future sessions: sync the plan table's status column at the same time as `execution-log.md`, not only at plan-append time.
- **The executor worker for ST-4 computed AC-15's empty-diff check and the full-story-diff scope against a stale `main` reference** (predating the `[E5.F2.H2]` merge, `8c080cb`), which would have spuriously included that already-merged story's files in the reported scope. Caught and corrected by the orchestrator, which re-ran both checks against the correct merge-base (`origin/main` at `8c080cb`) before accepting the stage as closed — the true scope (11 files) was confirmed independently, not trusted from the executor's self-report.
- **A latent, pre-existing defect discovered as a side effect of D5's empirical verification, not introduced by this story**: both merged E4 engine `process-runner.ts` seams (`claude-code`, `opencode`) carry a doc comment asserting "Only a genuine spawn failure (ENOENT, permission denied) REJECTS" — verified false for `execa@9.6.1` under `reject:false`. Out of scope for this story (AC-15 pins both adapters untouched); recorded as `risk-005` for a future backlog item rather than fixed here.
- **Full-4r code review found 1 confirmed CRITICAL** (R4-001, `evidence_class: deterministic`, no refuter pass applicable — verified by direct code reading instead) and 6 non-blocking `info`-tier findings (1 WARNING each from R1/R3×2, 1 SUGGESTION each from R2/R3, 1 WARNING from R4 recorded as `risk-007` — no process-group kill, so a forking command's grandchildren can outlive the timeout, deliberately deferred). R4-001 fixed in ST-5, independently mutation-proven (fix reverted → regression test failed for the exact claimed reason → re-applied → suite clean).
- **Final QA surfaced two new low-severity findings** beyond the review ledger (QA-101: AC-2's real-child test doesn't itself assert pre-kill output survival, though AC-1's test incidentally covers the same property; QA-102: re-confirms the ledger's own R3-001 — no test exercises the adapter's relative-`cwd` rejection path). Both non-blocking, recorded as `risk-008`/`risk-009`.

## Work done

- **Project-state validation** (start of session): confirmed PR #70 (`[E5.F2.H2]`) `merged: true` via direct `pull_request_read` (the batch `list_pull_requests` result showed a stale `merged: false` for the same PR — a real API-field discrepancy worth remembering, not a bug in this repo). Confirmed 0 open PRs, E5 issue states (`#33`/`#34` closed, `#31`/`#32` open, `#35` open/optional) match `history/INDEX.md` exactly. No discrepancies found; nothing needed fixing.
- **`[E5.F1.H1]` full cycle** (`b01774b`..`ed4b5a4`, 12 commits, all on `claude/e5-f1-h1-process-runner`):
  - Artifacts: `b01774b` proposal · `0b1ee47` spec revision 2 (17 ACs, empirical execa re-derivation) · `e99d444` design (7 decisions) · `4e1f715` plan (4 stages)
  - Implementation: `2544748` ST-1 (`ProcessRunner` port, pre-flight, 2 new errors) · `1ac03c1` ST-2 (pure `classifyExecaResult`, all 4 rules mutation-proven) · `e62e603` ST-3 (impure adapter shell + sixth `ProcessRunner.contract.ts`) · `046ca31` ST-4 (14 real-child tests incl. AC-1's load-bearing reaping proof, closing gate)
  - Review & fix: `0322adf` full-4r code review (verdict `fail`→1 confirmed CRITICAL) · `23f85ee` plan updated with ST-5 · `c53f5ff` ST-5 (R4-001 fixed, mutation-proven) · `ed4b5a4` final QA (`pass_with_warnings`, 17/17 ACs, change `completed`)
- **Validations**: `npm run check` (biome + `tsc --noEmit` + depcruise) green at every stage; `npm test` 375 → 388 → 399 → 403 → 401 (net; exact per-stage deltas in `execution-log.md`), each independently re-confirmed fresh at final QA. Final diff: 11 files, 997 insertions / 3 deletions, confined to `src/core/run/**` (5 files) and `src/adapters/driven/exec/**` (6 files) — zero spill into `src/core/run/run-review.ts`, any other adapter, or `src/main/` (AC-15, independently re-verified twice: once at ST-4's closing gate, once at final QA, both against a freshly re-derived merge-base).
- **Full-4r review, 4 lenses launched in parallel via the `Agent` tool** (`subagent_type: Explore`, read-only): R1 Risk 1 WARNING; R2 Readability 1 SUGGESTION; R3 Reliability 0 correctness defects + 2 WARNING/1 SUGGESTION coverage gaps; R4 Resilience 1 CRITICAL (R4-001) + 1 WARNING (`risk-007`). Every empirical claim in every lens's own findings was independently spot-checked by the orchestrator against `execa`'s actual source/types before merging — not taken on the lens's word.
- **This story shipped uncalled**, same deliberate deferral as `resolveEngine` (`[E4.F2.H3]`) and `RunStore` (`[E5.F2.H1]`/`[E5.F2.H2]`) before it — no composition-root wiring, no caller in `runReview` or anywhere else.

## Pending and next steps

- **User**: decide whether to open a PR for `[E5.F1.H1] ProcessRunner` (`Closes #31`). Per the workflow contract, Claude does not open a PR unless explicitly asked, and never merges.
- **User (optional, non-blocking)**: 6 ledger `info`-tier findings + 2 QA-tier low findings remain recorded as optional follow-up — most notably `risk-005` (a latent pre-existing defect in both merged E4 engine adapters' spawn-failure doc comments, worth its own backlog item) and `risk-007` (no process-group kill, so a forking validation command's grandchildren can outlive the timeout).
- **E5 milestone**: `[E5.F1.H2]` (declared validations in the review flow, #32) is now unblocked and is the only remaining required E5 story. `[E5.F2.H3]` (cost/tokens, #35) stays ⚪ optional. Not yet fully in open/merged PRs, so workflow contract rule 6 (epic summary + STOP) does not trigger.
- **Downstream constraints for `[E5.F1.H2]`**, recorded in `spec.md` and restated at final QA: (1) define the `validations: string[]` → `(command, args)` conversion without ever invoking a shell; (2) `run-review.ts`'s failure classifier currently falls through `InvalidProcessRequestError` to `"engine-error"` instead of `"validation-failed"` — `[E5.F1.H2]` must fix this since AC-15 pinned `run-review.ts` untouched in this story; (3) own the secret-exposure boundary for validation output reaching the prompt (this port captures raw output with no redaction, by design — D2's rationale).

## Open questions for the user

—
