# S26 — [E6.F1.H1] Base command CLI, end to end

- **Date**: 2026-08-24
- **Branch**: `claude/validar-estado-proyecto-rcvz8c`
- **Scope**: project-state validation (PR #72 merged, E5 required stories complete) + `[E6.F1.H1]` Base command CLI (issue #36), first story of E6 — full lite flow, 13 execution stages plus a review-fix round, four QA reviews, change `completed`. PR #73 open with all five review threads answered and resolved.
- **sdd-lite changes**: [`e6-f1-h1-cli-base`](../../sdd-lite/openspec/changes/e6-f1-h1-cli-base/)

## Objective

Confirm the project's state after PR #72 merged, then deliver `[E6.F1.H1]`: the scriptable CLI surface (`repo add|list`, `review`, `runs list|show`, `--version`, `--help`) plus the real composition root, replacing the `[E0.F1.H3]` `--version` stub. Until this story, the whole review pipeline existed but nothing was reachable from a terminal.

## Decisions

| ID | Decision | Alternatives considered | Why | Authorship |
|----|----------|-------------------------|-----|------------|
| S26-D0 | Refresh the sdd-lite bootstrap surgically before starting, rather than running full `sddl-init` or accepting the staleness. | Full `sddl-init` (heavier, may overwrite the CLAUDE.md wrapper block); accept staleness. | `config.yaml`'s own recorded refresh trigger — "E5, or the first E6 runtime dependency" — had fired twice over. `skill-catalog.md` was propagating E0-E3 evidence into every delegated worker. | `claude→user` |
| S26-D1 | Run persistence is owned by a new `persistRun` use case in `core/history`. | `runStore` in `RunReviewDeps`; compose in `src/main/`; defer to a new E5 story. | `RunStore.save()` had **no non-test caller**: neither `[E4.F1.H1]` nor `[E5.F2.H1]` assigned an owner, so `review` would have written no history and `[E5.F2.H2]`'s `listRuns` would read a store nothing writes. Level C (backlog gap, rule 8). Composing in the command would break this story's own AC-1. | `claude→user` |
| S26-D2 | Sentinel home root = `SENTINEL_HOME` when set and non-empty, else `~/.sentinel`. | Fixed `~/.sentinel`; a global `--home` flag. | Lets `[E7.F1.H1]`'s e2e suite point at a temp dir without mutating the real `HOME`; stays scriptable in CI. | `claude→user` |
| S26-D3 | `reviewTimeoutMs` as an optional `GlobalConfigSchema` field + `--timeout`, precedence flag > config > `DEFAULT_REVIEW_TIMEOUT_MS`. | A composition-root constant; a mandatory flag. | Mirrors how `validationTimeoutMs` already resolves. A non-configurable timeout in a CLI that invokes agents becomes a bug report. | `claude→user` |
| S26-D4 | `commander` is the only new runtime dependency; `picocolors` deferred to `[E6.F2.H2]`. | Both now; `node:util parseArgs` with zero deps. | Keeps a command-surface story to one dependency. `parseArgs` would mean hand-rolling subcommands and per-command `--help` — precisely this story's AC-2. | `claude→user` |
| S26-D5 | A pure `resolveReviewRequest` in `core/run`, sibling to `resolveEngine`, owns the flag → `RepoEntry` → `GlobalConfig` cascade. | Cascade in the CLI adapter; a `getRepo` use case plus in-command merging. | Nothing owned composing the ten-field `RunReviewRequest`, and `core/repos` exports no single-repo lookup. Leaving it in the adapter would force `[E6.F2.H1]`'s TUI to duplicate the precedence rules. | `claude→user` |
| S26-D6 | No `--json` in this story; stdout/stderr separation with a stable line format, question deferred to `[E6.F1.H2]`. | Ship `--json` now; rule it out permanently. | A JSON payload shape is a public API commitment; #37 is the scripting story that will actually have a consumer to validate it. | `claude→user` |
| S26-D7 | Module-private, idempotent alias→storage-key normaliser in `core/history` (`owner/repo` → `owner__repo`), applied by `persistRun`, `listRuns`, `getRun`. | Export a public `toRunRepoName`; change `deriveAlias` at the root; use only the last path segment. | `deriveAlias` emits `owner/repo` while `RunStore`'s `PathSegmentSchema` **rejects path separators** — every review and every `runs list` would have failed. Keeps the rule inside the module owning the persistence contract. **Changes behaviour of two use cases merged by `[E5.F2.H2]`.** | `claude→user` |
| S26-D8 | `SENTINEL_OPENCODE_MODEL` supplies the opencode model, failing with a one-liner when unset. | A `model` field in `GlobalConfigSchema`; drop opencode from the wiring. | Avoids a second config-format change in one story and hardcodes no model id that would drift. | `claude→user` |
| S26-D9 | Accept the D7 normalisation leak on the error path (`Run not found: owner__repo/x`); revisit at `[E7.F1.H2]`. | Fix in `getRun`; branch per error type in the CLI. | Cosmetic and confined to an error path. Fixing it would be a fourth core behaviour change for a stderr string; per-error branching is forbidden by AC-13. | `claude→user` |
| S26-D10 | `npm run dev` becomes `tsup --silent && node dist/cli.js`; four documents updated. | Add `tsx`; migrate ~97 modules to `.ts` specifiers; defer and document. | Level C: reality refuted a documented assumption in four files. `--experimental-strip-types` does not rewrite `.js` specifiers, and the stub never imported source, so the breakage surfaced exactly at S9. No new dependency; measured rebuild ~0.7s. | `claude→user` |
| S26-D11 | Fix the remote-only-branch git defect inside this story (S10b + S10c), with the missing contract coverage. | A new E2 issue, blocking or not; document the limitation. | `review` is the command this story exists to deliver; merging it broken for its primary use case is shipping something that does not work. Guidance recorded that dropping `--detach` is the **wrong** fix — it would create local branches in the managed clone, contradicting PRD §5.1. | `claude→user` |
| S26-D12 | Batch the stage approvals (S1-S4, S5-S7, S8-S9) rather than approving 11 stages individually, with an explicit stop before S10's smoke. | Stage by stage; everything through S11 in one go. | The batch removes the confirmation pause, not the per-stage execution boundary or the green-gate requirement. The user reserved S10's transcript deliberately. | `claude→user` |
| S26-D13 | Act on all five PR #73 review findings before merge, via a plan-routed fix stage (S12). | Fix only R4-001; fix R4-001 + R2-003; merge as-is and answer the threads. | All five were verified against source; none was a false positive. R4-001 is correctness with real user impact; R2-003 is a one-line seam inconsistency; R2-001/R2-002 close a drift class by copying a pattern already present in the same directory. The project's severity floor says WARNING/SUGGESTION never block, so acting was a choice — taken because they came from the human reviewer and were cheap. | `claude→user` |
| S26-D14 | A post-review persistence failure renders the verdict on stdout, one diagnostic on stderr, and exits non-zero. No sixth terminal state. | A new terminal state (the reviewer's suggestion); exit 0 on the literal reading of AC-12. | The five terminal states in PRD §4 are a domain contract; extending them from an adapter concern would be level C. AC-12 guarantees exit 0 for a *completed* invocation, and a run whose record never reached disk did not complete — so the guarantee is not violated. AC-12's text was amended to record the boundary case. | `claude→user` |

## Deviations

- **Four authorised scope widenings**, each beyond the backlog entry's literal text: **D1** (`persistRun`), **D5** (`resolveReviewRequest`), **D7** (normaliser, changing `[E5.F2.H2]`'s merged behaviour) and **D11** (git adapter fix, changing `[E2.F1.H2]`'s merged code). All user-authorised at checkpoints; all must be declared in the PR description. Final QA confirmed no undeclared fifth widening and no relaxed guard config.
- **`risk-e6h1-014` (high), found by S10's smoke, not by tests.** `git worktree add --detach <path> <branch>` fails for a branch existing only as `remotes/origin/<name>` — the normal case for a PR branch. **661 tests missed it** because `GitPort.contract.ts` defined its fixture branch as one existing *both* locally and as `origin/<name>`, the only shape where `--detach` works. Gap now closed at the port-contract level.
- **S10b's fix was scoped too narrowly, and that was the orchestrator's error, not the worker's.** The handoff said "fix `worktreeAdd`", treating the symptom's location as the boundary. The re-run smoke then died one stage later in `mergeBase` with the identical root cause. S10c completed it across all five ref-taking call sites after a whole-file audit.
- **The first smoke run passed and proved almost nothing.** The throwaway repo's HEAD sat on `feature`, so `baseRef == targetRef`, the diff was empty and the engine approved nothing. Recorded because it is the more instructive result: a green smoke over a degenerate fixture is worse than no smoke.
- **F-7, caught by the final QA review, was a gap in the orchestrator's own bookkeeping**: S10 was run inline, its results recorded as risk resolutions in `state.yaml`, but no `execution-log.md` entry was ever written — three risks cited evidence the log did not contain. Written retroactively in the closing commit.
- **The first final QA returned `pass` and the human reviewer then found five real issues it had missed**, one of them correctness (a `persistRun` throw discarding a completed review's verdict). The change was reopened from `completed` to `implementing` for a fix round (S12) and a second final review. Worth recording plainly: a clean automated `pass` is not the same as a clean review.
- **F-9, caught by the second final QA, was another orchestrator bookkeeping slip**: `spec.md`'s AC-12 was amended in the same commit whose execution-log entry said the spec text had not been updated — true when the stage wrote it, false when the commit landed. Corrected.
- `npm run dev` was broken by S9 and unnoticed until then (D10) — the `[E0.F1.H3]` stub never imported source code, so no earlier story could have surfaced it.

## Work done

- **Bootstrap refresh**: `922b1bd` (`config.yaml` + `project-context.md` to post-E4/E5 reality), `fdf0477` (`skill-catalog.md`, which every delegated worker reads as authoritative).
- **Planning artifacts**: `0c1fc5e` proposal · `ec3c8fa` spec (14 ACs) · `3fc1fef` design · `c8a84b9` plan (11 ordered stages).
- **Execution**, 13 stages: `f0de92a` S1 tooling · `a20dd5a` S2 `resolveReviewRequest` · `bd97131` S3 normaliser · `40a6deb` S4 `persistRun` · `41421c9` S5 CLI shell · `c00e210` S6 `repo`/`runs` · `81cab90` S7 `review` · `db9499f` S8 `paths.ts` · `8a7ccab` S9 composition root · `70672d6` S9b dev script · S10 smoke (inline) · `98793de` S10b + `ffb6816` S10c git ref resolution · `d0e9b08` S11 closeout.
- **Three QA reviews**: `9516598` stage mode over S1-S4 (`pass`, 3 info) · `80c3a48` stage mode over S5-S7 (`pass`, 2 info) · `d0e9b08` **final mode, verdict `pass`, change `completed`**. The S5-S7 review independently reproduced three stages' mutation-verification claims rather than trusting them.
- **Validations**: `npm run check` exit 0 (143 files, 97 modules / 226 deps, 0 violations) · `npm test` exit 0, **674 tests across 38 files, up from 500/28** · `npm run build` exit 0 · `npm run dev -- --version` exit 0.
- **S10 smoke, end to end against a real repo and a real engine**: clone → remote-only ref resolution → worktree → 1-file/11-line diff via `merge-base(main, feature)..feature` → 220-line prompt → `claude-code` → verdict `request-changes` → persistence → read-back. The engine flagged the planted `eval()` as a blocker. Transcript in the change's `execution-log.md`.
- **PR #73 opened**, then a review round: five threads from the repo owner, all verified genuine, all fixed in `2633f51` (S12), each thread answered with its evidence and resolved. Second final QA re-run: verdict `pass`, change `completed` again — it mutated the new guards four ways to confirm they fail for the defect they name, and judged the AC-13 two-stderr-lines question explicitly rather than waving it through.
- Closing state: 31 commits, **681 tests across 38 files**, `check`/`test`/`build` all exit 0, 18 risks recorded (9 resolved, 9 open, all by decision).

## Pending and next steps

- **User**: merge PR #73. All five review threads are answered and resolved; nothing is outstanding on it. Never merged by Claude.
- **Nine open risks**, all by recorded decision. From the review round: `risk-e6h1-016` (two stderr lines on a persistence failure — QA judged it not an AC-13 violation, tidy for `[E6.F1.H2]`) · `-017` (`renderRecord` binds by length, not key) · `-018` (stale `review_ledger` path in `state.yaml`, pre-existing). From the story itself: `risk-e6h1-008` (commander needs Node ≥22.12 vs declared ≥22; matters at `[E7.F2.H3]`) · `-009` (renderers must echo the typed alias — a standing rule for `[E6.F2.x]`) · `-010` (D9, error-path leak) · `-011` (`repo add` prints `-` for a cloned repo's local path — **the change's one literal deviation from a spec behaviour row**) · `-012` (`review` omits the run id) · `-015` (empty `worktrees/<repo>/` parent survives cleanup).
- **QA finding F-6**, open for `[E7.F1.H1]`: `container.ts` (218 lines of hot-path wiring) has no automated test, and `createEngine`'s `opencode` branch has never executed.
- **Next stories in E6**: `[E6.F1.H2]` (#37, exit codes — also inherits the `--json` question), then `[E6.F2.H1]`/`[E6.F2.H2]` (TUI).

## Open questions for the user

—
