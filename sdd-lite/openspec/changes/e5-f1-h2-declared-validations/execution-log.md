# Execution Log

## ST-1 — Additive config field: `validationTimeoutMs`

- **Status**: completed
- **Scope**: `src/core/repos/ports/config-schemas.ts` (edit), `src/core/repos/__test__/config-schemas.test.ts` (new)
- **What landed**:
  - `validationTimeoutMs: z.number().optional()` appended to both `GlobalConfigSchema` and `RepoEntrySchema`, each with a doc comment recording *why* there is deliberately no `.default()` and no numeric-range guard here: the single fallback constant (`DEFAULT_VALIDATION_TIMEOUT_MS`) lives in `run`, and the R2-2 numeric-range guard (finite, `> 0`, `≤ 2_147_483_647`) is pinned by spec.md AC-4/design.md D-4 to `runReview`'s stage-1 pre-flight, not to the schema — confirmed by re-reading spec.md's AC-5 text verbatim before writing any code, per the R2-2 instruction in this stage's handoff. `RepoEntry.validations` was left completely untouched — still exactly `z.array(z.string()).optional()`.
  - `src/core/repos/__test__/config-schemas.test.ts` (new — no prior schema test file existed for this module, per spec.md R2-6's legal-home finding): 7 tests across `GlobalConfigSchema` and `RepoEntrySchema` — a pre-story document still parses with `validationTimeoutMs` `undefined` (and no own-property present, ruling out a hidden default), a document carrying the field parses with the value preserved, and one explicit assertion that `validations` stays unwidened (accepts a string array, rejects an object-shaped entry).
- **Validation**:
  - `npx vitest run --project core -t "config-schemas"`: `Test Files 1 passed | 16 skipped (17)`, `Tests 7 passed | 202 skipped (209)`.
  - `npm run check` (`biome check . && tsc --noEmit && depcruise src`): `Checked 115 files in 252ms. No fixes applied.` / `tsc --noEmit` clean (no output) / `✔ no dependency violations found (80 modules, 164 dependencies cruised)`.
- **Deviations**: none. The R2-2 guard-placement question in the handoff was resolved by reading spec.md's AC-5 text directly ("The config schema deliberately does **not** enforce this... the stage-1 pre-flight is the only gate") rather than assumed — the schema field is correctly a bare `.optional()` on both schemas, and no validation logic was added at this stage.

## ST-2 — Pure-function surface: `runValidations`

- **Status**: completed
- **Scope**: `src/core/run/run-validations.ts` (new), `src/core/run/run-errors.ts` (edit), `src/core/run/index.ts` (edit, `run-validations` exports only), `src/core/run/__test__/fake-process-runner.ts` (new), `src/core/run/__test__/run-validations.test.ts` (new).
- **What landed**:
  - `run-errors.ts`: `InvalidValidationDeclarationError extends RunError` appended, mirroring `InvalidRunRequestError`/`InvalidProcessRequestError` — no `cause`, doc comment naming AC-7/AC-8 as the two triggers.
  - `run-validations.ts` (new, single production file per design.md D-1): the four module-private constants (`DEFAULT_VALIDATION_TIMEOUT_MS = 120_000`, `VALIDATION_HEAD_LINES = 100`, `VALIDATION_TAIL_LINES = 100`, `VALIDATION_MAX_LINE_CHARS = 2_000`); the pinned rejection set `REJECTED_SHELL_CHARS` as a literal `Set` plus `isRejectedChar`'s codepoint-range predicate for the control-character half (D-3); `tokenizeDeclaration` (exported) scanning characters first, then splitting on `/[ \t]+/` after trimming, then rejecting a zero-token result (D-2/D-3), with the message template pinned verbatim in design.md; `validateValidationDeclarations` (exported) running `tokenizeDeclaration` over the whole list before anything spawns, discarding the result (D-2 — the pre-flight IS the tokenizer, defined once, reused by the stage-1 hoist ST-3 will add); `windowStream`/`windowStreamOrEmpty` applying D6's head+tail line window with R2-4's line semantics (split on `\n` alone; a trailing `\n`'s own empty split segment is not a line) and the `(empty)` substitution ahead of the window (D-5); `formatOutcomeElement`/`formatSpawnFailureElement` assembling AC-14's byte-exact element via explicit concatenation and the `terminated()` helper, never `parts.join("\n")` (D-6, R2-5); `runValidations` itself — pre-flight over the whole list, then a sequential `for…of` with one `await` and one `catch (error: unknown)` that recovers only `ProcessSpawnError` (R2-1), letting `InvalidProcessRequestError` and anything else propagate.
  - `__test__/fake-process-runner.ts` (new, design.md D-7 — deliberately NOT added to `run-review-fixtures.ts`, which AC-18 forbids importing from the new test): `createFakeProcessRunner` over a scripted `resolve`/`reject` outcome queue, recording `{ command, args, cwd, timeoutMs }` per call and throwing if a second `run()` starts while one is still `inFlight` — proves AC-2's non-overlap without a timing race. `okResult()` helper for a clean-exit baseline result.
  - `index.ts`: barrel now exports exactly `runValidations`, `RunValidationsRequest`, `RunValidationsDeps`, `RunValidationsResult`, `validateValidationDeclarations`, and `InvalidValidationDeclarationError` (added to the existing `run-errors.js` export list) — nothing from `run-review.ts` (that is ST-3). `biome`'s `assist/source/organizeImports` re-sorted the new export block alphabetically among the file's existing exports; accepted as a formatter-driven, semantically inert reordering.
  - `__test__/run-validations.test.ts` (new, 71 tests, one outer `describe("run-validations", …)` wrapper so `vitest -t "run-validations"` selects the whole file): table-driven tokenizer/rejection-set coverage (AC-6, AC-7 — one case per pinned character plus accept cases for `- = . / : , + @ %`, AC-8), `validateValidationDeclarations`'s whole-list/bad-entry-anywhere behavior (AC-10), sequential ordering + non-overlap + per-call `cwd` (AC-2, AC-3), supplied/default timeout (AC-4), the three never-abort paths plus the R2-1 propagation case (AC-11, AC-12, AC-13), exact-string assertions on both the normal and spawn-failure element formats including a trailing-newline and a no-trailing-newline body (AC-14), the D6 window's 300-line/250-line/200-line-boundary/over-long-line cases (AC-15), and a two-run determinism check with a no-timestamp assertion (AC-21). AC-18 (standalone testability) is proven structurally by the file's own import list — only `run-errors.js`, `run-validations.js`, and the local fake.
- **Validation**:
  - `npx vitest run --project core -t "run-validations"`: `Test Files 1 passed | 17 skipped (18)`, `Tests 71 passed | 209 skipped (280)`.
  - `npm run check` (`biome check . && tsc --noEmit && depcruise src`): first pass surfaced three biome formatting/import-order findings (long single-line `elidedCount` expression, `run-errors.js`/`run-validations.js` export-block sort order in `index.ts`, and reflowed indentation of the new test file caused by the added outer `describe` wrapper) plus one `exactOptionalPropertyTypes` error in the test file (`okResult({ exitCode: undefined, … })` — `Partial<ProcessRunResult>` under `exactOptionalPropertyTypes` rejects an explicit `undefined` for a non-optional field). Fixed via `npx biome check --write` for the formatting/import findings and by replacing that one test case's `okResult({ exitCode: undefined, … })` call with a literal `ProcessRunResult` object omitting the `exitCode` key entirely (the correct way to express "field absent" under this compiler flag). Second pass: `Checked 118 files in 114ms. No fixes applied.` / `tsc --noEmit` clean / `✔ no dependency violations found (81 modules, 168 dependencies cruised)`.
  - `npm test` (full suite, sanity check per the handoff — ST-3 hasn't wired anything yet, so `run-review.ts` is untouched): `Test Files 28 passed (28)`, `Tests 479 passed (479)`.
  - `git status --porcelain` after the stage: exactly the five files this stage's Expected Scope names (`src/core/run/index.ts` M, `src/core/run/run-errors.ts` M, plus the three new files) — no drift into `run-review.ts` or any other module.
- **Mutation testing** (plan.md's ST-2 note):
  - AC-7's rejection set: temporarily replaced `isRejectedChar`'s literal-`Set` lookup with a regex `/[|&;<>$`(){}[\]*?!~#\\'"=]/` that also matches `=`. Re-ran the targeted suite: the two `--foo=bar`-shaped accept-case tests failed as expected (`accepts entries containing "="` and `accepts a real-world flag entry (--foo=bar shape)`, both threw `InvalidValidationDeclarationError` instead of passing). Reverted to the literal `Set` + codepoint predicate; re-ran — clean (`71 passed`).
  - AC-14/AC-15's formatter: temporarily replaced the `terminated()`-based explicit concatenation in `formatOutcomeElement` with an array + `.join("\n")` (`[header1, header2, "--- stdout ---", out.text, "--- stderr ---", err.text].join("\n")`). Re-ran: 5 tests failed, including both exact-string AC-14 assertions on a body already ending in `\n` (a spurious blank line appeared before `--- stderr ---`, as R2-5 predicted) and the AC-15 window-boundary tests whose stderr already ended in `\n`. Reverted to the `terminated()`-based concatenation; re-ran — clean.
  - AC-14/AC-15's `elided` flag: temporarily dropped `out.elided`/`err.elided` from `formatOutcomeElement`'s `truncated` computation (kept only the two capture-truncation flags). Re-ran: the 300-line-stdout window-overflow test's `truncated=true` assertion failed as expected (element rendered `truncated=false` despite eliding 100 lines). Reverted to the four-way OR; re-ran — clean (`71 passed`).
  - All three mutations were reverted before the final `npm run check`/`npm test` runs recorded above; the working tree at close of this stage contains no mutation residue.
- **Deviations**: none from plan.md/spec.md/design.md. Two implementation decisions made at Level A during coding, recorded here per the sdd-lite audit-history rule (technical, reversible, aligned with the pinned design):
  - `tokenizeDeclaration`'s `command` extraction uses `tokens[0] as string` after the `tokens.length === 0` guard, mirroring the existing `as string` precedent at `src/core/review/load-harnesses.ts:53` for a known-nonempty array under `noUncheckedIndexedAccess` — not a new pattern in this codebase.
  - The test file's raw control-character literals (`"npm\x00test"`, `"npm\x7ftest"`, `"npm\x01test"`) are written as explicit `\x` escapes rather than literal bytes, after an initial draft accidentally embedded literal NUL/DEL bytes in the source file (caught by a byte-scan before running any check) — escapes are the only safe way to express these test fixtures in a UTF-8 source file.
- **Open risks carried forward** (not this stage's to resolve): risk-006 (process-group kill hardening) and risk-007 (no secret redaction) remain accepted and documented in spec.md, unaffected by this stage — `runValidations` neither hardens nor worsens either.
