version: "0.1"
change_name: e5-f2-h2-query-history
objective: new-feature
mode: lite
complexity_assessment:
  route: continue-lite
  summary: >-
    Story [E5.F2.H2] (issue #34), second story of the E5 milestone: give the history core module
    its read side -- listRuns and getRun use cases over the RunStore port, which [E5.F2.H1] left
    write-only (save() alone). The on-disk layout (runs/<repo>/<ts>/, metadata.json, the
    .partial-<ts> staging convention) is fully specified by the merged [E5.F2.H1]. The use-case
    pattern is directly precedented by listRepos/listBranches ([E2.F2.H3]). Risk is concentrated
    in one open design question rather than in mechanics: what "corrupt/partial, listed with a
    marker" means precisely, given that [E5.F2.H1]'s own AC-19 scanning convention already skips
    dot-prefixed .partial-<ts> entries rather than surfacing them -- the literal opposite of what
    this story's acceptance criterion asks for.
  confidence: medium
  recorded_at: "2026-08-22T18:00:00Z"
lifecycle_status: reviewing
current_stage: sddl-executor
stages:
  sddl-deep-explorer:
    status: skipped
    notes: >-
      No blocking unknown. [E5.F2.H1]'s port, fs adapter and layout module are all directly
      readable, as is the E2.F2.H3 listRepos/listBranches precedent; lightweight proposal-stage
      exploration was sufficient.
  sddl-proposal:
    status: completed
    started_at: "2026-08-22T17:50:00Z"
    completed_at: "2026-08-22T18:00:00Z"
    notes: >-
      proposal.md written grounded in direct inspection of src/core/history/index.ts and
      ports/run-store.ts (RunStore currently write-only, save() alone), src/adapters/driven/
      storage/run-layout.ts and run-store-fs.ts ([E5.F2.H1]'s on-disk layout, timestamp format,
      and .partial-<ts> staging convention), src/core/repos/list-repos.ts and list-branches.ts
      (the structural template for a thin, dependency-injected read use case), and
      docs/backlog-mvp-sentinel.md's E5.F2.H2 entry. Four open questions recorded for spec, two
      B-level: the precise definition of "corrupt/partial" given [E5.F2.H1]'s AC-19 already skips
      .partial-<ts> entries during scanning, and the shape of RunStore's new read method(s).
      [E5.F2.H3] (cost/tokens) treated as out of scope per workflow contract rule 7.
    last_updated_at: "2026-08-22T18:00:00Z"
  sddl-spec:
    status: completed
    started_at: "2026-08-22T18:10:00Z"
    completed_at: "2026-08-22T19:00:00Z"
    notes: >-
      spec.md written, 12 acceptance criteria at rev 1. All four proposal open questions resolved
      with recommendations: D1 (both .partial-<ts> staging leftovers AND corrupt-metadata final
      directories are surfaced via RunSummary.status, deliberately diverging from [E5.F2.H1]'s
      AC-19 scan which skips dot-prefixed entries for its narrower collision-check purpose); D2
      (list() returns a distinct, mostly-optional RunSummary rather than RunRecord[], because a
      partial/corrupt entry cannot supply a full RunRecord); D3 (getRun returns file bodies, not
      paths, symmetric with what save() accepted); D4 (empty repo -> [], not an error, closed
      A-level). Grounded in direct inspection of RunStore.contract.ts, run-store-errors.ts,
      terminal-state.ts, verdict.ts, and list-branches-errors.ts.
      REVISION 2 (user-directed second iteration before ratification): D1..D4 survived unchanged;
      five rev-1 gaps fixed, now 15 ACs. (1) Rev 1's AC-6 named a nonexistent validation target
      ("RunRecord-shape validation") -- metadata.json is a distinct document (version, repo, ISO
      startedAt) with no schema anywhere; new D6/D7 introduce RunMetadataSchema with a
      compile-time satisfies drift guard against the run unions, since AC-15 pins src/core/run
      untouched. (2) No path-traversal validation existed on read inputs -- list/get turn caller
      strings into path segments; new AC-13 mirrors save()'s RunRecordPathFieldsSchema
      discipline and requires rejection BEFORE any fs access. (3) No raw-error translation AC for
      read paths (no analogue of H1's AC-20); new AC-14. (4) Rev 1's scan was implicitly two-way,
      so a stray .DS_Store would have been listed as a corrupt run; new D9/AC-12 make the
      classification three-way (ts-dir -> run, .partial-<ts> -> partial, anything else silently
      ignored), plus unknown metadata version -> corrupt (AC-6) and tampering-induced final+
      partial coexistence -> final wins (AC-4). (5) Under-specified data sourcing pinned: id
      semantics (D5), startedAtEpochMs uniformly from the directory name (D8), diff.warnings
      defaulting to [] on round-trip, validations/*.log read order sorted by filename (AC-9).
      Timestamp inverse-parse round-trip verified with node before writing AC-2.
    last_updated_at: "2026-08-22T19:00:00Z"
  sddl-design:
    status: completed
    started_at: "2026-08-22T19:10:00Z"
    completed_at: "2026-08-22T19:35:00Z"
    notes: >-
      design.md written: 5 new files, 6 modified, 6 design decisions (D-1..D-6), 0 blocking open
      questions, full AC-to-test-file coverage map for all 15 ACs. Key choices: RunMetadataSchema
      mirrors the persisted document with version z.literal(1) and unknown-key strip (additive
      version-1 fields stay readable; breaking changes must bump version, which the literal turns
      into corrupt); the TerminalState/Verdict/RunStage literal arrays carry a two-direction
      compile-time drift guard (satisfies for rogue values, Expect<Exclude<...>> for
      exhaustiveness) because AC-15 forbids relocating the unions into src/core/run; a new
      InvalidRunQueryError instead of overloading InvalidRunRecordError (a query is not a
      record); RunPersistenceError broadened to cover raw fs failures on reads rather than
      minting a fourth translation class; the id's ts-format check lives in the adapter's
      parseRunTimestamp pre-fs (a core-schema regex would leak the adapter's layout format into
      the port contract -- core validates segment-safety only); and a pure classifyRunDirEntry
      in run-layout.ts concentrates D9's three-way classification and D8's timestamp sourcing in
      one function testable without a filesystem. Use cases stay listRepos-thin; all validation
      lives behind the port so every future RunStore is equally bound.
    last_updated_at: "2026-08-22T19:35:00Z"
  sddl-plan:
    status: completed
    started_at: "2026-08-22T19:45:00Z"
    completed_at: "2026-08-22T20:05:00Z"
    notes: >-
      plan.md written: 4 executor stages, sequenced exactly per design.md's own recommendation
      (core-first, then pure adapter functions, then fs read flows plus contract thickening, then
      planted-state tests as the closing gate). ST-1 core surface (port extension, RunMetadataSchema
      with drift guards, 3 new errors plus broadened RunPersistenceError, 2 use cases, barrel);
      ST-2 pure parseRunTimestamp/classifyRunDirEntry with fs-free unit tests proving D8/D9 before
      any I/O exists; ST-3 impure list()/get() flows plus RunStore.contract.ts thickening (closing
      [E5.F2.H1]'s risk-004); ST-4 fs-specific planted-state tests (partial/corrupt/stray/dedupe/
      injection) and the story's closing gate, re-verifying AC-15's empty src/core/run diff and
      all four architecture guards. 9 files total across the 4 stages. No manual verification
      stage -- pure fs I/O against a temp dir plus pure functions, fully testable in CI.
    last_updated_at: "2026-08-22T20:05:00Z"
  sddl-executor:
    status: completed
    started_at: "2026-08-22T20:15:00Z"
    notes: >-
      ST-1 (core surface) completed. RunStatus/RunSummary + list/get on RunStore;
      RunMetadataSchema with the version-literal + unknown-key-strip persisted-document schema
      and the two-direction satisfies/Expect<Exclude<...>> drift guard; InvalidRunQueryError/
      RunNotFoundError/RunCorruptedError added, RunPersistenceError doc broadened;
      RunQueryFieldsSchema added, repoName's refinement factored into a shared
      PathSegmentSchema; listRuns/getRun use cases mirroring listRepos/listBranches; barrel
      updated. npm run check green (depcruise: 75 modules, 151 deps, 0 violations), npm test
      331/331 (326+5). git diff --stat -- src/core/run empty. Drift-guard proof: removed
      validation-failed from TERMINAL_STATES, tsc failed exactly at the Expect<Exclude<...>>
      line (TS2344), reverted, re-verified clean.
      One flagged deviation: touched src/adapters/driven/storage/run-store-fs.ts (unplanned for
      ST-1) with throwing list()/get() stubs, because extending the RunStore interface breaks
      tsc against its existing sole implementor -- no way to keep npm run check green at the
      stage boundary otherwise. Treated as A-level (technical, reversible). ST-3's scope is
      unchanged (replaces the stub bodies rather than adding new methods).
      Mechanical biome import-order fix applied via --write to index.ts and get-run.test.ts
      (same recurring deviation class [E5.F2.H1]'s stages recorded).
      ST-2 (pure adapter functions) completed: parseRunTimestamp (exact inverse of
      formatRunTimestamp, null not throw on malformed input) and classifyRunDirEntry (D9's
      three-way rule, D5's id-without-prefix addressing contract) in run-layout.ts. npm run
      check green (depcruise unchanged: 75 modules, 151 deps), npm test 339/339 (331+8). No
      deviation -- exactly the planned 2 files. Non-vacuity proven by mutation: short-circuited
      the .partial- branch condition, only the partial-classification test failed (for the
      right reason), the other 4 classifyRunDirEntry tests stayed green, reverted and
      re-verified clean. git diff --stat -- src/core/run empty.
      ST-3 (impure list()/get() flows + RunStore.contract.ts thickening) completed. list()
      scans+classifies+validates+merges (final wins)+sorts; get() validates, resolves via the
      shared readMetadata helper, distinguishes RunNotFoundError vs RunCorruptedError via the
      sibling .partial-<id> check, reads optional bodies, defaults diff.warnings to []. npm run
      check green (depcruise: 76 modules, 152 deps), npm test 346/346 (339+7). No deviation
      beyond one exactOptionalPropertyTypes fix (explicit per-field usage reconstruction instead
      of a direct zod-inferred assignment) -- mechanical, no behavior change. git diff --stat --
      src/core/run empty.
      IMPORTANT finding from mutation attempt 1: removing list()'s explicit .sort() did NOT fail
      any contract test -- the real fs happened to return readdir entries already in ascending
      order for this fixture, so the contract suite cannot prove AC-2's readdir-order-independence
      claim; that proof needs a mocked readdir (design's own AC-2 validation hint) and is
      deferred to ST-4 as originally planned, not silently discovered there. Sort call kept
      (still correct/defensive). Attempt 2 (usage.inputTokens reconstruction) succeeded as a
      real non-vacuity proof.
      ST-4 (planted-state tests + closing gate) completed: 14 new tests across 5 describe blocks,
      all planting real on-disk state directly (mkdirSync/writeFileSync) rather than only via
      save(). Covers AC-4 (partial inclusion + final-wins dedupe), AC-6 (all 4 corrupt cases:
      missing/invalid-json/missing-field/wrong-version), AC-7 (mixed listing), AC-11 (get() on
      partial/corrupt), AC-12 (stray entries ignored), AC-13 (path-traversal rejection, no-fs-
      access proof), AC-14 (raw fs failure translation via vi.doMock), and AC-2's real proof
      (mocked readdir returning reverse order, list() still ascending -- the test ST-3's
      attempt-1 mutation showed was missing). npm run check green (depcruise: 76 modules, 152
      deps, unchanged), npm test 360/360 (346+14). AC-17 verified: grep for process.env across
      all production files in src/core/history and src/adapters/driven/storage, zero matches.
      Non-vacuity proven by mutation: swapped the list() merge order so partial would win over
      final, the new coexistence test failed for the right reason, reverted, re-verified clean.
      CLOSING GATE: git diff --stat -- src/core/run empty against origin/main (AC-15 holds for
      the whole story); full story diff exactly the 14 code files plan.md's ST-1..ST-4 named
      collectively, confined to src/core/history/** and src/adapters/driven/storage/**;
      depcruise confirms all 4 architecture guards. All 15 ACs from spec.md revision 2 covered.
      plan.md's ST-1..ST-4 fully executed.
      FIX ROUND 1 (2026-08-22T22:35:00Z): ST-5 appended per fix-stage-request rules (append one
      bounded stage, do not rebuild the plan): scope is exactly review-ledger.md's R3-001 and
      R4-001, one atomic work unit per id. R3-001 fix: get()'s "missing" branch now also checks
      exists(finalDir) before the staging-sibling fallback, so a final dir that exists but lost
      metadata.json throws RunCorruptedError (matching list()'s classification) instead of
      RunNotFoundError. R4-001 fix: list()'s per-entry readMetadata catch now classifies that
      one entry as corrupt instead of rethrowing out of the loop, resolving the AC-7/AC-14
      tension in favor of AC-7's "none affecting the others" per the review's own finding --
      recorded as an A-level decision (technical, reversible, no scope change) since the exact
      mechanism is an implementation detail within the already-authorized fix scope. ST-1..ST-4
      statuses corrected from pending to completed in plan.md's table (plan-authoring artifact,
      not a re-open -- all four were already done).
      ST-5 (fix stage) completed at 2026-08-22T22:50:00Z. R3-001 fix: get()'s missing branch
      checks exists(finalDir) first. R4-001 fix: list()'s per-entry catch degrades that one
      entry to corrupt instead of rethrowing (A-level decision resolving the AC-7/AC-14 tension
      in favor of AC-7, per the review's own recommendation). npm run check green (depcruise: 76
      modules, 152 deps, unchanged), npm test 362/362 (360+2 new regression tests). Exactly the
      2 planned files touched, no deviation. Non-vacuity proven by mutation for BOTH fixes
      independently: reverting each one made its dedicated regression test fail for the exact
      claimed reason, re-applying restored a clean suite. git diff --stat -- src/core/run empty.
      review-ledger.md updated: both findings open -> fixed, verdict fail -> pass_with_warnings,
      0 open severe findings, 4 info rows remain (non-blocking).
    last_updated_at: "2026-08-22T22:50:00Z"
  sddl-qa-review:
    status: pending
artifacts:
  state: sdd-lite/openspec/changes/e5-f2-h2-query-history/state.yaml
  proposal: sdd-lite/openspec/changes/e5-f2-h2-query-history/proposal.md
  spec: sdd-lite/openspec/changes/e5-f2-h2-query-history/spec.md
  design: sdd-lite/openspec/changes/e5-f2-h2-query-history/design.md
  plan: sdd-lite/openspec/changes/e5-f2-h2-query-history/plan.md
  execution_log: sdd-lite/openspec/changes/e5-f2-h2-query-history/execution-log.md
  review_ledger: sdd-lite/openspec/changes/e5-f2-h2-query-history/review-ledger.md
  qa_report: sdd-lite/openspec/changes/e5-f2-h2-query-history/qa-report.md
checkpoints:
  - id: cp-proposal-approval
    type: stage_approval
    stage: sddl-proposal
    summary: >-
      proposal.md is complete and ready for sddl-spec. Two B-level questions are recorded for spec
      to resolve as firm acceptance criteria before any design work: the precise meaning of
      "corrupt/partial, listed with a marker," and the shape of RunStore's new read method(s).
    question: >-
      Approve the proposal and proceed to sddl-spec for [E5.F2.H2] Query history?
    free_input_allowed: true
    created_at: "2026-08-22T18:00:00Z"
    response:
      free_text: "si, avanza con el spec"
      responded_at: "2026-08-22T18:05:00Z"
    resolved_at: "2026-08-22T18:05:00Z"
  - id: cp-spec-approval
    type: stage_approval
    stage: sddl-spec
    summary: >-
      spec.md is complete at revision 2 with 15 acceptance criteria (a user-directed second
      iteration fixed five rev-1 gaps -- see the spec's Revision Notes -- and added A-level
      D5..D9 covering id semantics, RunMetadataSchema, the satisfies drift guard, uniform
      timestamp sourcing, and the three-way scan classification). Two B-level decisions (D1
      corrupt/partial definition, D2 read-method shape) are recommended and need ratification
      before sddl-design.
    question: >-
      Ratify D1 (list() surfaces both .partial-<ts> leftovers and corrupt-metadata final
      directories via RunSummary.status) and D2 (list() returns a distinct RunSummary, not
      RunRecord[]; get() returns the full RunRecord), and proceed to sddl-design?
    options:
      - id: opt-ratify-both
        label: Ratify D1 and D2 and proceed to design
        description: >-
          Accept both as specified. list()/get() ship with the RunSummary/RunRecord split and the
          dual partial+corrupt marker.
        recommended: true
      - id: opt-d1-partial-only
        label: Reject D1 partially -- only surface .partial-<ts>, not corrupt metadata
        description: >-
          Narrower scope: treat metadata parse/validation failure as an unexpected error instead
          of a listed status. Simpler, but AC-6/AC-7 would need to change to "list() may throw on
          a corrupt entry," which is closer to the failure mode the AC explicitly warns against.
      - id: opt-d2-full-record
        label: Reject D2 -- list() returns RunRecord[] instead of RunSummary[]
        description: >-
          Keeps one type instead of two, but forces partial/corrupt entries to fabricate
          placeholder values for RunRecord's required fields (harness, baseRef, targetRef, state),
          which has no honest value to use.
    free_input_allowed: true
    created_at: "2026-08-22T18:35:00Z"
    response:
      selected_option_id: opt-ratify-both
      free_text: "ratifico las dos, avanza con el design"
      responded_at: "2026-08-22T19:10:00Z"
    resolved_at: "2026-08-22T19:10:00Z"
  - id: cp-design-approval
    type: stage_approval
    stage: sddl-design
    summary: >-
      design.md is complete: 5 new files, 6 modified, 6 design decisions, 0 blocking open
      questions, and an explicit AC-to-test-file coverage map for all 15 ACs. Notable A-level
      refinements over the spec sketch: a new InvalidRunQueryError (rather than overloading
      InvalidRunRecordError), RunPersistenceError broadened to read-path raw failures, and the
      id ts-format check placed in the adapter's parseRunTimestamp rather than a core schema.
    question: >-
      Approve design.md -- including the InvalidRunQueryError addition and the broadened
      RunPersistenceError scope -- and proceed to sddl-plan?
    free_input_allowed: true
    created_at: "2026-08-22T19:35:00Z"
    response:
      free_text: "si, avanza con el plan"
      responded_at: "2026-08-22T19:45:00Z"
    resolved_at: "2026-08-22T19:45:00Z"
  - id: cp-plan-approval
    type: stage_approval
    stage: sddl-plan
    summary: >-
      plan.md is complete: 4 executor stages (ST-1..ST-4), each with its own stage_approval gate
      to come during sddl-executor. Sequencing matches design.md's own recommendation with no
      deviation. ST-4 is the closing gate and the only stage that re-verifies AC-15 (empty diff
      over src/core/run).
    question: >-
      Approve plan.md and proceed to sddl-executor, which will request separate approval before
      each of ST-1 through ST-4 touches code?
    free_input_allowed: true
    created_at: "2026-08-22T20:05:00Z"
    response:
      free_text: "ejecutar todo modo auto"
      responded_at: "2026-08-22T20:10:00Z"
    resolved_at: "2026-08-22T20:10:00Z"
  - id: cp-st1-approval
    type: stage_approval
    stage: sddl-executor
    summary: >-
      ST-1 (core surface) approved to start by the blanket "ejecutar todo modo auto"
      authorization, and completed cleanly: npm run check and npm test both green (331/331),
      diff scoped to the plan.md file list plus one flagged deviation (stub list/get in the
      existing fs adapter, required for tsc to pass against the extended interface), drift-guard
      proof by mutation, src/core/run untouched.
    question: >-
      ST-1 complete. Proceed to ST-2 (pure parseRunTimestamp/classifyRunDirEntry)?
    free_input_allowed: true
    created_at: "2026-08-22T20:35:00Z"
    response:
      free_text: "ejecutar todo modo auto"
      responded_at: "2026-08-22T20:10:00Z"
    resolved_at: "2026-08-22T20:35:00Z"
  - id: cp-st2-approval
    type: stage_approval
    stage: sddl-executor
    summary: >-
      ST-2 (pure adapter functions) approved to start by the same blanket auto-mode
      authorization, and completed cleanly: npm run check and npm test both green (339/339), no
      deviation, non-vacuity proven by mutation, src/core/run untouched.
    question: >-
      ST-2 complete. Proceed to ST-3 (impure list()/get() flows, replacing ST-1's stubs, plus
      RunStore.contract.ts thickening)?
    free_input_allowed: true
    created_at: "2026-08-22T20:55:00Z"
    response:
      free_text: "ejecutar todo modo auto"
      responded_at: "2026-08-22T20:10:00Z"
    resolved_at: "2026-08-22T20:55:00Z"
  - id: cp-st3-approval
    type: stage_approval
    stage: sddl-executor
    summary: >-
      ST-3 (impure list()/get() flows + contract thickening) approved to start by the same
      blanket auto-mode authorization, and completed cleanly: npm run check and npm test both
      green (346/346), no deviation beyond one exactOptionalPropertyTypes mechanical fix,
      src/core/run untouched. Mutation testing surfaced a real finding (not silently passed
      over): the contract suite cannot prove AC-2's readdir-order-independence on this
      environment's filesystem behavior -- that proof is confirmed deferred to ST-4's mocked
      readdir test, per design's own validation hint.
    question: >-
      ST-3 complete. Proceed to ST-4 (fs-specific planted-state tests, including the
      readdir-order-independence proof, and the story's closing gate)?
    free_input_allowed: true
    created_at: "2026-08-22T21:20:00Z"
    response:
      free_text: "ejecutar todo modo auto"
      responded_at: "2026-08-22T20:10:00Z"
    resolved_at: "2026-08-22T21:20:00Z"
  - id: cp-st4-approval
    type: stage_approval
    stage: sddl-executor
    summary: >-
      ST-4 (planted-state tests + closing gate) approved to start by the same blanket auto-mode
      authorization, and completed cleanly: npm run check and npm test both green (360/360),
      non-vacuity proven by mutation, AC-17 verified by inspection with zero process.env hits.
      Closing gate confirms AC-15 (empty src/core/run diff) and all 4 architecture guards across
      the full 14-file story diff. All 15 ACs covered.
    question: >-
      plan.md's ST-1..ST-4 are complete. Proceed to sddl-code-review (4R protocol) before
      sddl-qa-review?
    free_input_allowed: true
    created_at: "2026-08-22T21:50:00Z"
    response:
      free_text: "ejecutar todo modo auto"
      responded_at: "2026-08-22T20:10:00Z"
    resolved_at: "2026-08-22T21:50:00Z"
  - id: cp-review-gate
    type: review_gate
    stage: sddl-code-review
    summary: >-
      full-4r review complete over the whole 14-file story diff (1296 changed lines). 4 lens
      sweeps, 0 refuter passes needed (both severe findings are deterministic, independently
      re-verified by the orchestrator through direct code reading before merging). Verdict: fail
      -- 2 confirmed open CRITICAL findings, both real correctness defects introduced by this
      story: R3-001 (get() misreports a corrupted run -- final dir exists, metadata.json absent,
      no .partial- sibling -- as RunNotFoundError instead of RunCorruptedError, contradicting
      list()'s classification of the identical on-disk state) and R4-001 (list() aborts the
      ENTIRE listing when ONE entry hits a raw non-ENOENT fs error while reading its
      metadata.json, instead of degrading just that entry -- contradicting AC-7's "none
      affecting the others" intent). 4 additional WARNING/SUGGESTION findings recorded as info,
      non-blocking (get()/list() closure decomposition, unfiltered stray files in validations/,
      lexicographic vs numeric sort past 999 validation-log entries).
    question: >-
      Both confirmed findings are inside this change's approved scope (real defects in code this
      story wrote, not a scope-exceeding discovery). Suggested route per fix-routing protocol:
      rerun sddl-plan to insert one bounded fix stage covering exactly R3-001 and R4-001, then
      stage_approval, then sddl-executor. The blanket "ejecutar todo modo auto" authorization
      covered the already-approved ST-1..ST-4 plan, not a new fix stage -- authorize this fix
      round?
    options:
      - id: opt-fix-both
        label: Authorize a fix stage for R3-001 and R4-001
        description: >-
          Insert one bounded ST-5 into plan.md fixing both confirmed findings, gated by its own
          stage_approval, then a scoped re-review against this frozen ledger.
        recommended: true
      - id: opt-defer
        label: Defer the fix -- proceed to final QA with findings still open
        description: >-
          Not recommended: both findings are real, deterministic correctness defects with
          concrete user-facing consequences (a corrupted run reported as nonexistent; a repo
          with one bad entry losing its entire run history listing).
    free_input_allowed: true
    created_at: "2026-08-22T22:20:00Z"
    response:
      selected_option_id: opt-fix-both
      free_text: "Sí, arreglá ambos (recomendado)"
      responded_at: "2026-08-22T22:30:00Z"
    resolved_at: "2026-08-22T22:30:00Z"
  - id: cp-st5-approval
    type: stage_approval
    stage: sddl-executor
    summary: >-
      ST-5 (fix stage for R3-001, R4-001) appended to plan.md's Stage Plan per the fix-routing
      protocol, authorized by the user's "Sí, arreglá ambos" at cp-review-gate. This single
      response covers both the fix-stage-insertion decision and this stage's own stage_approval,
      since the user's choice was already specific to authorizing exactly this fix work.
    question: >-
      Start ST-5: fix R3-001 (get()'s missing-metadata misclassification) and R4-001 (list()
      aborting on one entry's raw fs error), with new regression tests for both?
    free_input_allowed: true
    created_at: "2026-08-22T22:35:00Z"
    response:
      free_text: "Sí, arreglá ambos (recomendado)"
      responded_at: "2026-08-22T22:30:00Z"
    resolved_at: "2026-08-22T22:35:00Z"
decisions:
  - id: dec-001
    checkpoint_id: cp-proposal-approval
    free_text: "si, avanza con el spec"
    rationale: >-
      User approved the proposal and directed the spec stage to run. Authorship: claude->user.
    recorded_at: "2026-08-22T18:05:00Z"
  - id: dec-002
    checkpoint_id: cp-spec-approval
    selected_option_id: opt-ratify-both
    free_text: "ratifico las dos, avanza con el design"
    rationale: >-
      User ratified both B-level decisions as recommended, against spec revision 2 (which the
      user had directed as a second re-analysis iteration before ratifying): D1 (list() surfaces
      both .partial-<ts> staging leftovers and corrupt-metadata final directories via
      RunSummary.status, diverging deliberately from [E5.F2.H1]'s AC-19 collision-check scan)
      and D2 (list() returns a distinct mostly-optional RunSummary; get() returns the full
      RunRecord). The A-level D5..D9 closures added by revision 2 were ratified implicitly with
      the same approval. Authorship: claude->user.
    recorded_at: "2026-08-22T19:10:00Z"
  - id: dec-003
    checkpoint_id: cp-design-approval
    free_text: "si, avanza con el plan"
    rationale: >-
      User approved design.md (including the InvalidRunQueryError addition and the broadened
      RunPersistenceError scope, both flagged explicitly at the checkpoint) and directed the plan
      stage to run. Authorship: claude->user.
    recorded_at: "2026-08-22T19:45:00Z"
  - id: dec-004
    checkpoint_id: cp-plan-approval
    free_text: "ejecutar todo modo auto"
    rationale: >-
      User approved plan.md and gave blanket auto-mode authorization covering ST-1 through ST-4.
      Per CLAUDE.md, auto mode chains stages without confirmation pauses but never bypasses
      stage_approval for code-touching stages; this blanket instruction is treated as the
      explicit stage_approval for each of ST-1..ST-4 individually, recorded per-stage as it
      completes. Orchestrator will still stop and surface any blocked/partial result or a
      medium+ severity risk rather than pushing through silently. Authorship: claude->user.
    recorded_at: "2026-08-22T20:10:00Z"
  - id: dec-005
    checkpoint_id: cp-review-gate
    selected_option_id: opt-fix-both
    free_text: "Sí, arreglá ambos (recomendado)"
    rationale: >-
      User authorized a bounded fix stage for both confirmed CRITICAL findings (R3-001, R4-001)
      per the recommended option. Route: rerun sddl-plan to insert one fix stage, gated by its
      own stage_approval, then sddl-executor, then a scoped re-review against the frozen ledger.
      Authorship: claude->user.
    recorded_at: "2026-08-22T22:30:00Z"
open_risks:
  - id: risk-001
    summary: >-
      CLOSED. D1 ratified at cp-spec-approval (dec-002): list() surfaces both .partial-<ts>
      staging leftovers and corrupt-metadata final directories via RunSummary.status,
      deliberately diverging from [E5.F2.H1]'s AC-19 scan (which skips dot-prefixed entries for
      its own narrower collision-check purpose). Implemented as designed in ST-1's RunSummary
      shape; scan logic itself lands in ST-2/ST-3.
    severity: low
  - id: risk-002
    summary: >-
      CLOSED by ST-3. ST-1 added throwing list()/get() stubs to createRunStoreFsAdapter
      (unplanned for ST-1, required to keep tsc green against the extended RunStore interface).
      ST-3 replaced both stub bodies with the real implementation, so the exposure no longer
      exists — confirmed no caller of RunStore existed anywhere in the repo during the window
      the stubs were live.
    severity: low
  - id: risk-003
    summary: >-
      CLOSED by ST-4. list()'s explicit .sort() was unproven by the portable contract suite on
      this environment's filesystem (readdir happened to already return ascending order for the
      test fixture). ST-4 added the real proof design's AC-2 validation hint called for: a
      mocked readdir forced to return entries in reverse chronological order, asserting list()
      still returns them ascending.
    severity: low
  - id: risk-004
    summary: >-
      CLOSED by ST-5. review-ledger.md R3-001: get() misreported a corrupted run (final dir
      exists, metadata.json absent, no .partial- sibling) as RunNotFoundError instead of
      RunCorruptedError. Fixed: get() now checks exists(finalDir) before the staging-sibling
      fallback. Non-vacuity proven by mutation (revert -> dedicated regression test fails for
      the exact reason -> re-apply -> clean).
    severity: low
  - id: risk-005
    summary: >-
      CLOSED by ST-5. review-ledger.md R4-001: list() aborted the entire listing when ONE entry
      hit a raw non-ENOENT fs error reading its metadata.json. Fixed: that one entry now
      degrades to status corrupt instead of aborting the call (A-level decision resolving the
      AC-7/AC-14 tension in favor of AC-7's graceful-degradation intent). Non-vacuity proven by
      mutation.
    severity: low
review_summary:
  mode: 4r
  verdict: pass_with_warnings
  round: 1
  counts:
    confirmed: 0
    suspect: 0
    escalated: 0
    info: 4
  summary: >-
    full-4r tier (1296 changed lines). 4 lens sweeps: R1 clean; R2 found 2 info-tier
    decomposition suggestions; R3 found 1 CRITICAL (get()/list() classification mismatch) plus 2
    WARNING robustness gaps in validation-log reading; R4 found 1 CRITICAL (list() aborts
    entirely on one entry's raw fs error). 0 refuter passes -- both CRITICALs deterministic,
    independently re-verified by the orchestrator before merging. Both CRITICALs fixed in ST-5
    (user-authorized at cp-review-gate), each fix independently proven by mutation. 0 open
    severe findings remain; 4 info rows (non-blocking) carried forward as optional follow-up.
  reported_at: "2026-08-22T22:50:00Z"
  ledger_path: sdd-lite/openspec/changes/e5-f2-h2-query-history/review-ledger.md
next_action:
  kind: run_stage
  target: sddl-qa-review
  summary: >-
    ST-5 fix stage complete, both CRITICAL findings fixed and verified. review-ledger.md verdict
    pass_with_warnings, 0 open severe findings. Proceeding to sddl-qa-review in final mode.
created_at: "2026-08-22T17:50:00Z"
updated_at: "2026-08-22T21:50:00Z"
