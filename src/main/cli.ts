/**
 * Composition root: future CLI entrypoint — the only place adapters are
 * instantiated (PRD §4.2). Wiring lands in E6.F1.x.
 * Executing this file is a deliberate no-op: an empty ESM module exits 0.
 */
export {};
